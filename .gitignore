/agonsr/settings.toml

# Bytecode. mcts.py is run as a script by the WebUI's dispatcher, so this
# appears next to it on its own.
__pycache__/
*.pyc

# Working notes from a design phase, and never this repository's to carry: the
# two that were here planned the web front end, which lives in another repo
# entirely. They are a second, staler answer to questions the code and its own
# docs now answer, and they go out with a push nobody meant to make. The files
# stay on disk; this only stops them coming back.
docs/superpowers/

# Problem workspaces. Locally this holds every problem, its data and its runs;
# the public repository carries only the README that says what goes here.
/artifacts/*
!/artifacts/README.md
